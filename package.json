{
    "name": "portfolio",
    "version": "1.0.0",
    "scripts": {
        "build": "mkdir -p dist && npx cleancss -o dist/style.css src/style.css && cp src/index.html dist/ && cp src/profile.jpg dist/"
    },
    "devDependencies": {
        "clean-css-cli": "^5.6.2"
    }
}